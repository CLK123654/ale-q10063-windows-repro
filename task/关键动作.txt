查看SQLite源表、报告窗口、裁决顺序和报表合同，理清候选与订阅者、活动、历史送达及人工抑制的关联。
换算候选请求的本地时刻，按订阅状态、历史幂等、人工抑制、用户等级和静默时段完成前置裁决。
计算同一订阅者、活动、渠道和dedupe_key下的去重窗口，再累计用户日配额与活动报告窗配额。
编写完成版SQLite脚本，重建候选裁决、发送计划、抑制审计、活动配额和运行元数据表。
从同一SQLite数据库导出发送计划、抑制审计和活动配额报告，使字段与排序符合report_schema.json。
查询数据库完整性、候选归属和活动余量，核对派生表与三份报表中的业务记录。
