查看五张SQLite源表及三个规则文件，确认报告窗口、裁决次序、原因文案和报表字段之间能够互相对应。

把UTC请求时间换成本地时刻，先处理订阅者、活动、历史幂等、人工抑制、等级和静默时段，再让仍合格的候选进入去重与配额计算。

按候选先后累计用户日配额，随后按活动优先级和请求顺序占用活动额度，使每个event_id只落入发送计划或抑制审计中的一处。

重建SQLite派生表和索引，从同一数据库导出发送计划、抑制明细及活动配额报告，并把报告窗口和汇总数写入repair_meta。

重复执行入口，核对源表未变、数据库完整、三份报表与派生表一致；缺少规则文件时确认进程失败且不遗留动态交付物。
